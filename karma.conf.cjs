/* eslint-disable no-console */
/* eslint-disable no-unused-vars */
/* eslint-disable func-names */
// Karma configuration

'use strict';

const { setupKarmaConfig } = require('./karma.base.cjs');

module.exports = function (config) {
  setupKarmaConfig(config);
};
