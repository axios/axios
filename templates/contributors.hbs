{{#if authors}}

  ### Contributors to this release

  {{#each authors}}
    {{#unless isBot}}
    {{#if login}}
      - {{#if avatar_url}}![avatar]({{avatar_url_sm}}){{/if}} [{{displayName}}]({{html_url}})
    {{else}}
      - {{displayName}}
    {{/if}}
    {{/unless}}
  {{/each}}

{{/if}}
